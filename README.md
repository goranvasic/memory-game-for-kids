# Memory Game for Kids

Requires Python 3.x

Run the game with the following command:

```bash
python -m http.server 8000
```

Open your web browser and navigate to `http://localhost:8000` to start playing the game.
